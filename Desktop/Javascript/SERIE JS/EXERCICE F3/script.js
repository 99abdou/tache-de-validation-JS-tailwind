let nombre = parseInt(prompt("Veuillez entrer un nombre :"));

// Calculez le modulo de ce nombre divisé par 5
let module = nombre % 5;

// Affichez le résultat dans une phrase
alert("Le reste de la division de " + nombre + " par 5 est égal à " + module);
